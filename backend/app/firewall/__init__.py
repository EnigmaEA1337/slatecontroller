"""Firewall manipulation module — UCI helpers for hardening rules.

Today: DNS anti-bypass (block client DoT + activate GL.iNet drop_leaked_*).
Future: per-network egress rules, GeoIP whitelist for lockdown profile, etc.
"""
